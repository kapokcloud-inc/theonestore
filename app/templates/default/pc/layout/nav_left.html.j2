<div class="navL-wrap">
  <div class="allPad">
    <div class="navL-box">
      <div class="navL-box_hd">{{_('订单中心')}}</div>
      <ul class="navL-box_bd">
        <li>
          <a {{ libs.get_me_navclass('order') }} href="{{ url_for('pc.order.index', tab_status=0) }}">{{_('我的订单')}}</a>
        </li>
        <li>
          {% set uncomment_count = '(%s)' % g.uncomment_count|string() if g.uncomment_count else '' %}
          <a {{ libs.get_me_navclass('comment') }} href="{{ url_for('pc.order.comment', ip=1,ps=20,is_pending=1) }}">{{_('评价晒单')}} {{ uncomment_count }}</a>
        </li>
        <li>
          <a {{ libs.get_me_navclass('aftersales') }} href="{{ url_for('pc.aftersales.root') }}">{{_('售后服务记录')}}</a>
        </li>
      </ul>
    </div>
    <div class="navL-box">
      <div class="navL-box_hd">{{_('个人中心')}}</div>
      <ul class="navL-box_bd">
        <li>
          <a {{ libs.get_me_navclass('profile') }} href="{{ url_for('pc.me.index') }}">{{_('我的个人中心')}}</a>
        </li>
        <li>
          {% set unread_count = '(%s)' % g.unread_count|string() if g.unread_count else "" %}
          <a {{ libs.get_me_navclass('messages') }} href="{{ url_for('pc.me.messages') }}">{{_('消息通知')}} {{ unread_count }}</a>
        </li>
        <li>
          <a {{ libs.get_me_navclass('wallet') }} href="{{ url_for('pc.wallet.root') }}">{{_('钱包')}}</a>
        </li>
        <li>
          <a {{ libs.get_me_navclass('collect') }} href="{{ url_for('pc.me.collect',p=1,ps=20) }}">{{_('我的收藏')}}</a>
        </li>
        <li>
          <a {{ libs.get_me_navclass('coupon') }} href="{{ url_for('pc.me.coupon') }}">{{_('优惠券')}}</a>
        </li>
        <li>
          <a {{ libs.get_me_navclass('addresses') }} href="{{ url_for('pc.me.addresses') }}">{{_('收货地址')}}</a>
        </li>
      </ul>
    </div>
  </div>
</div>
