{% extends '/default/admin/layout/base.html.j2' %}
{% block body %}
<div id="page-wrapper">
  <div class="container-fluid">
    {{ libs.setting_page_tabs() }}

    <!-- .row -->
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <div class="white-box">
          <h3 class="box-title"><a target="_blank">{{ _('售后服务') }}</a></h3>
          <p class="text-muted m-b-30">
            {{ _('售后服务配置 - 用户退货/换货寄回商品的快递的寄件信息') }}
          </p>

          {{ libs.build_tabs(tabs=[
            {'label':_('售后服务'), 'endpoint':'admin.config.aftersales_server'},
          ], border_bottom='line') }}

          {# 售后服务 form表单 #}
          {{ libs.render_form(form, action=url_for('admin.config.aftersales_server')) }}

        </div>
      </div>
    </div>
    <!-- /.row -->
  </div>
</div>
{% endblock %}